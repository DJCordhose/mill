function isMoveValid(state, game) {
    const possibleNextStates = expandState(state, game)    
}

function expandState(state, game) {
    if (game.phase === 'Set') {

    } else if (game.phase === 'Moving') {

    }
}
